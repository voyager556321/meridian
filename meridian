#!/usr/bin/env bash
# Launch Meridian with the project venv when present.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -x "${ROOT}/.venv/bin/python" ]]; then
  exec "${ROOT}/.venv/bin/python" "${ROOT}/main.py" "$@"
fi
exec python3 "${ROOT}/main.py" "$@"
